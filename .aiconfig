# .aiconfig

project:
  name: QS-Ext-Jump-Start
  description: >
    Boilerplate for Qlik Sense extension development, featuring Nebula CLI, Playwright testing, and deployment guides.
  language: JavaScript
  frameworks:
    - nebula.js
    - playwright
  documentation: README.md
  knowledge_base: KNOWLEDGE_BASE.md
  project_structure: docs/PROJECT_STRUCTURE.md
  workflows: docs/WORKFLOWS.md
  license: SEE LICENSE IN license.txt

ai:
  preferred_model: claude-3-5-sonnet-20241022
  fallback_model: gpt-4o-mini
  cursor_compatible: true
  code_style:
    lint: eslint
    format: prettier
    editor: cursor
  policies:
    - name: pre-commit-validation
      description: 'All changes must pass lint and tests before committing.'
      steps:
        - run: npm run -s lint
        - run: npm test -s -- --reporter=list
      required: true
    - name: package-json-change-requires-install
      description: 'If package.json is modified, run npm install to update package-lock.json before committing.'
      when: changes-include('package.json')
      steps:
        - run: npm install --no-audit --no-fund
        - run: git add package-lock.json
      required: true
    - name: no-commit-without-lint
      description: 'Block committing if lint has not been executed in this session.'
      steps:
        - assert: last-ran('npm run -s lint')
      required: true
    - name: no-commit-without-tests
      description: 'Block committing if tests have not been executed in this session.'
      steps:
        - assert: last-ran('npm test -s -- --reporter=list')
      required: true
  prompt_templates:
    - 'Summarize this code.'
    - 'Update this template into a new project per docs/WORKFLOWS.md (Workflow B) and the Rename & Rebrand checklist, without changing runtime behavior.'
    - 'Suggest improvements for Qlik Sense extension development.'
    - 'Generate Playwright tests for this module.'
    - 'List files related to QAE configuration.'
    - 'Create documentation for a new property panel section.'
    - 'Explain how this Qlik Sense extension works to a new developer.'
    - 'Add TypeScript support to this extension.'
    - 'Optimize this code for performance and accessibility.'
    - 'Create a new property panel configuration.'
    - 'Debug this Playwright test failure.'
    - 'Generate JSDoc comments for this function.'
  cursor_specific:
    enable_chat_context: true
    enable_codebase_analysis: true
    preferred_completion_style: 'contextual'
    enable_multifile_editing: true
  exclude_paths:
    - node_modules/
    - dist/
    - test/report/
    - .git/
    - .cursor/
    - '*.log'

contributors:
  - name: Qlik Professional Services
  - name: Karthik Burra
    email: karthik.burra@qlik.com
  - name: Gabriel Wingard
    email: gabriel.wingard@qlik.com

default_context:
  # Qlik Sense and Nebula.js official resources
  - https://qlik.dev/ (Qlik Sense Developer Documentation)
  - https://qlik.dev/extend/ (Nebula CLI Documentation)

  # Qlik open source reference implementations
  - All repositories from https://github.com/qlik-oss with names starting with sn-

  # Core technology documentation
  - https://react.dev/reference/react/hooks (React Hooks)
  - https://playwright.dev/docs/intro (Playwright Testing Framework)
# Optionally, you can add more sections for integrations, security, or custom instructions.
